from setuptools import setup, find_packages

setup(
    name='SecCryPack',
    version='0.1',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        'cryptography',
    ],
)
